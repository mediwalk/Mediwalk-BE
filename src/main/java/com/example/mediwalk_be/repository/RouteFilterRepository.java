package com.example.mediwalk_be.repository;

import com.example.mediwalk_be.entity.RouteFilter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RouteFilterRepository extends JpaRepository<RouteFilter, Long> {
}
